const DashboardEmptyState = ({ message }) => (
    <div className="flex h-[220px] items-center justify-center rounded-xl border border-dashed border-slate-700 bg-slate-900/40 text-xs text-slate-500">
        {message}
    </div>
);

export default DashboardEmptyState;
